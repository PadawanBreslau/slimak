class Add<%= (column_name + "_to_" + file_name.pluralize).camelize %> < ActiveRecord::Migration[<%= migration_version_option %>]
  def change
    add_column :<%= file_name.pluralize %>, :<%= column_name %>, :jsonb, null: false, default: {}
    add_index  :<%= file_name.pluralize %>, :<%= column_name %>, using: :gin
  end
end
